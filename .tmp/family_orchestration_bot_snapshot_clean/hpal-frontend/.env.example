# HPAL Frontend Configuration

# HPAL Backend API Base URL
VITE_API_BASE_URL=http://localhost:8000/api

# Optional: Family ID (can also be passed via URL query param)
# VITE_DEFAULT_FAMILY_ID=default-family
